/* eslint-disable prefer-template */
/* eslint-disable quotes */
import styled from 'styled-components'
import { Button } from '@material-ui/core'

const StyledButton = styled(Button)``

export const ReButton = StyledButton
